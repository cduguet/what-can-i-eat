# Environment Variables Template for "What Can I Eat" Application
#
# Copy this file to .env and fill in your actual values
#
# To get Supabase values:
# 1. Go to https://supabase.com/dashboard
# 2. Create a new project or select existing project
# 3. Go to Settings > API
# 4. Copy the Project URL and anon/public key

# Supabase Configuration (Required)
EXPO_PUBLIC_SUPABASE_URL=https://nbworpqbjrkkfitmoggk.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5id29ycHFianJra2ZpdG1vZ2drIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NTU4ODA4ODQsImV4cCI6MjA3MTQ1Njg4NH0.nmQ5_GB_-xu-r2ar5WcQUt96RB00-KdhCtl_oF-D22E

# Supabase Backend Mode Configuration
# Set to 'supabase' to use Supabase Edge Functions for AI processing
# Set to 'local' to use direct AI API calls (default)
EXPO_PUBLIC_BACKEND_MODE=supabase

# AI Provider Configuration
# Choose which AI provider to use: 'gemini' or 'vertex'
# If not specified, defaults to 'gemini' for backward compatibility
EXPO_PUBLIC_AI_PROVIDER=gemini

# Gemini API Configuration (Required when AI_PROVIDER=gemini)
EXPO_PUBLIC_GEMINI_API_KEY=your-gemini-api-key-here
# Optional: Custom Gemini endpoint (uses default if not specified)
# EXPO_PUBLIC_GEMINI_ENDPOINT=https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash-latest:generateContent

# Vertex AI Configuration (Required when AI_PROVIDER=vertex)
# To get Vertex AI values:
# 1. Go to https://console.cloud.google.com/
# 2. Create or select a Google Cloud project
# 3. Enable the Vertex AI API
# 4. Create a service account with Vertex AI permissions
# 5. Download the service account key JSON file
EXPO_PUBLIC_VERTEX_PROJECT_ID=your-google-cloud-project-id
EXPO_PUBLIC_VERTEX_LOCATION=us-central1
# Service account credentials (JSON string or file path)
# Option 1: JSON string (recommended for production)
# EXPO_PUBLIC_VERTEX_CREDENTIALS={"type":"service_account","project_id":"..."}
# Option 2: File path (recommended for development)
# EXPO_PUBLIC_VERTEX_CREDENTIALS=/path/to/service-account-key.json

# Common AI Configuration (Optional)
# Request timeout in milliseconds (default: 30000)
# EXPO_PUBLIC_API_TIMEOUT=30000
# Maximum retry attempts (default: 3)
# EXPO_PUBLIC_MAX_RETRIES=3

# Development Configuration
NODE_ENV=development